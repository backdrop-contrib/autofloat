name = AutoFloat
description = A text format filter that adds an odd/even class to images and spans (image + caption) to make them float alternately left and right.
type = module
backdrop = 1.x
configure = admin/config/content/autofloat
